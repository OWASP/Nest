{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/OWASP/Nest/main/schema/chapter.json",
  "$defs": {
    "leaders": {
      "description": "Leaders of the project.",
      "type": "array",
      "items": {
        "$ref": "definitions/person.json"
      },
      "minItems": 2,
      "uniqueItems": true
    },
    "sponsors": {
      "description": "Sponsors of the project.",
      "type": "array",
      "items": {
        "$ref": "definitions/sponsor.json"
      },
      "minItems": 1
    },
    "social_media": {
      "description": "Social media information for the project",
      "type": "array",
      "items": {
        "$ref": "definitions/socialMedia.json"
      },
      "minItems": 1,
      "additionalProperties": false
    }
  },

  "additionalProperties": false,
  "description": "OWASP chapter schema.",
  "properties": {
    "blog": {
      "description": "Chapter's blog.",
      "format": "uri",
      "type": ["string"]
    },
    "country": {
      "description": "Country.",
      "type": "string"
    },
    "events": {
      "description": "Event URLs related to the chapter.",
      "type": "array",
      "items": {
        "format": "uri",
        "type": "string"
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "leaders": {
      "description": "Leaders of the chapter.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/leaders"
      },
      "minItems": 2,
      "uniqueItems": true
    },
    "meetup-group": {
      "description": "Meetup group.",
      "type": "string"
    },
    "name": {
      "description": "The unique name of the chapter.",
      "minLength": 10,
      "type": "string"
    },
    "region": {
      "description": "Region.",
      "type": "string"
    },
    "sponsors": {
      "description": "Sponsors of the chapter.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/sponsors"
      },
      "minItems": 1
    },
    "tags": {
      "description": "Tags for the chapter",
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 3,
      "uniqueItems": true
    },
    "website": {
      "description": "The official website of the chapter.",
      "type": "string",
      "format": "url"
    },
    "social_media": {
      "description": "Social media information for the chapter.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/social_media"
      },
      "minItems": 1,
      "additionalProperties": false
    }
  },
  "required": ["country", "leaders", "name", "tags"],
  "title": "OWASP Chapter",
  "type": "object"
}
