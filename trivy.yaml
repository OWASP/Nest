filesystem:
  skip-dirs:
    - /home/owasp/.venv/
    - /home/owasp/node_modules

ignorefile: trivyignore.yaml

scan:
  skip-dirs:
    - /home/owasp/.venv/
    - /home/owasp/node_modules

timeout: 10m

vulnerability:
  ignore-unfixed: true
  security-checks:
    - config
    - secret
    - vuln
  severity:
    - CRITICAL
    - HIGH
