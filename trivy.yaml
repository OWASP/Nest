filesystem:
  skip-dirs:
    - .venv
    - node_modules

ignorefile: trivyignore.yaml

scan:
  skip-dirs:
    - .venv
    - node_modules

timeout: 10m

vulnerability:
  ignore-unfixed: true
  security-checks:
    - config
    - secret
    - vuln
  severity:
    - CRITICAL
    - HIGH
