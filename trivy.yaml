filesystem:
  skip-files:

ignorefile: trivyignore.yaml

scan:
  skip-files:

severity:
  - CRITICAL
  - HIGH
  - UNKNOWN

show-suppressed: true

timeout: 10m

vulnerability:
  ignore-unfixed: true
  security-checks:
    - misconfig
    - secret
    - vuln
