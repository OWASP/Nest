exit-code: 1

filesystem:
  skip-files:

ignorefile: /trivyignore.yaml

report: all

scan:
  show-suppressed: true
  skip-dirs:
    - .cache
  skip-files:

severity:
  - CRITICAL
  - HIGH
  - UNKNOWN

timeout: 10m

vulnerability:
  ignore-unfixed: true
