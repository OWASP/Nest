filesystem:
  skip-files:

ignorefile: trivyignore.yaml

scan:
  skip-files:
  scanners:
    - misconfig
    - secret
    - vuln

severity:
  - CRITICAL
  - HIGH
  - UNKNOWN

show-suppressed: true

timeout: 10m

vulnerability:
  ignore-unfixed: true
