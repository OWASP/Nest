"""Recognition app config."""

from django.apps import AppConfig


class RecognitionConfig(AppConfig):
    """Recognition app config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.recognition"
