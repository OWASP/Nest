rules:
  dangerous-triggers:
    ignore:
      # Checks out base branch only; does not execute PR head code.
      - check-pr-issue.yaml
      # Labels PRs from configuration; does not checkout PR code.
      - pr-labeler.yaml
