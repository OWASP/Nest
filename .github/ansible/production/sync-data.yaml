- name: Run Nest Production Data Sync
  hosts: production_nest
  tasks:
    - name: Sync Nest data
      async: 72000  # 20 hours
      poll: 0
      ansible.builtin.shell:
        cmd: make sync-data > /var/log/nest/production/sync-data.log 2>&1
