- name: Run Nest Staging Data Update
  hosts: nest_staging
  tasks:
    - name: Update Nest data
      async: 10800 # 3 hours
      poll: 60
      register: update_data
      shell:
        cmd: 'make update'

    - name: Show the output
      debug:
        var: update_data
